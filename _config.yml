# Root Jekyll configuration for the app_public_pages GitHub Pages site.
# This is the ONLY _config.yml that GitHub Pages reads — sub-folder configs are ignored.
# Page-specific settings (title, icon, description) are defined in each index.md's front matter.
title: "App Public Pages"
description: "Public-facing pages for iOS apps"
url: "https://bartlettbutter.github.io"
baseurl: "/app_public_pages"
